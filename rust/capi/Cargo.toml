[package]
name = "hyperon_capi"
version = "0.1.0"
authors = ["Vitaly Bogdanov <vsbogd@gmail.com>"]
edition = "2018"

[dependencies]
hyperon = { path = "../lib/" }

[lib]
name = "hyperon_capi"
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib"]
