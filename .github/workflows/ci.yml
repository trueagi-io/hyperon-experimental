name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  tests:
    uses: ./.github/workflows/build.yml
    with:
      python-version: '3.7'
      os: ubuntu-20.04
      make-packages: false
